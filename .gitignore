# =========================================================
# FRONTEND ANGULAR — o que NÃO vai para o git
# Quem clonar roda: npm install  (recupera node_modules do package-lock.json versionado)
# =========================================================

## Dependências — geradas via npm install
node_modules/

## Build / compilação — geradas via ng build / npm run build
dist/
.tmp/
.tmp/
out/
out-tsc/
server/
.server/
.prerendered-routes.json
3rdpartylicenses.txt

## Caches do build
.angular/
.cache/
.parcel-cache/
.vite/
.sass-cache/
.turbo/
.tsbuildinfo

## Cobertura de testes
coverage/
.nyc_output/

## IDEs (per-user) — cada dev usa a sua
.idea/
.vscode/
.history/
.project
.classpath
.c9/
*.launch
.settings/
*.sublime-workspace

## System
.DS_Store
Thumbs.db

## Env local
.env
.env.*
!.env.example

## Arquivos de dados temporários (uploads/testes)
*.csv
*.xlsx
*.xls
*.pdf
downloads/
